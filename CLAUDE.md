# KEPT — Claude Code notes

> **Canonical standards:** `EGS-STANDARDS.md` in the private `egs-platform` root repo — read §1–§2 before building. A request conflicting with Tier 1 = stop and say so.

Read `EGS-STANDARDS.md` §1 (Tier 1) and §2 (the every-app checklist) before
building anything here. That file is the source of truth; this one stays lean.

- Ship via `egs-deploy.sh` from this repo — `--quick` routine, `--full` before anything real.
- Working procedure (deploy, fix-scripts, the bare-push exception): the root `CLAUDE.md` in the private `egs-platform` repo.
- Why a decision was made: `docs/EGS-DECISIONS.md` in the private `egs-platform` repo.
- App state, versions, launch status: `EGS-PORTFOLIO.md` in the private `egs-platform` repo.

App-specific notes go below this line — not copies of the standards.
